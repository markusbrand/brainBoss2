# GEMINI_API_KEY: Required for Gemini AI story quest generation & textbook scanner.
GEMINI_API_KEY=""

# APP_URL: The public URL where this app is hosted (optional in dev).
APP_URL=""

# VITE_SUPER_ADMIN_EMAIL: (Optional) Configures the designated super administrator email
VITE_SUPER_ADMIN_EMAIL=""

# Optional Database Configuration (Only required if connecting to an external/production PostgreSQL):
# If not provided, BrainBoss automatically falls back to browser localStorage mode.
# DATABASE_URL="postgres://brainboss:brainboss_pass@postgres:5432/brainboss"

